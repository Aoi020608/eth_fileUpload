// mod move_zero;
// mod meeting_rooms;
mod is_subsequence;


#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
