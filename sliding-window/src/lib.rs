// mod longest_substring_without_repeating_characters;
// mod minimum_size_subarray_sum;
mod permutation;

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
