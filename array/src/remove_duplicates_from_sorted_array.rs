#[allow(dead_code)]
pub fn remove_duplicates(nums: &mut Vec<i32>) -> i32 {
    let mut expected_nums: Vec<i32> = vec![];
    let compared_num = 0;

    for num in nums.iter() {
        if *num == compared_num {
            
        }
    }

    0
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_remove_duplicates() {

    }
}